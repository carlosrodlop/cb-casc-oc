jenkins:
  systemMessage: "Hello World! from Casc"
credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              description: "GH-token"
              id: "GH-token"
              password: ${githubToken}
              scope: GLOBAL
              username: ${githubUser}
              usernameSecret: true
