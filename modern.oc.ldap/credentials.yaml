credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          description: "GH-token"
          id: "GH-token"
          password: ${githubToken}
          scope: GLOBAL
          username: ${githubUser}
          usernameSecret: true
      - string:
          description: "GH-ST-token"
          id: "GH-ST-token"
          scope: GLOBAL
          secret: ${githubToken}
